import QtQuick
import QtQuick.Layouts
import QtQuick.Controls.Basic

Base {
    Label {
        text: "Mass and Balance"
        anchors.centerIn: parent
        font.pixelSize: 16
        font.bold: true
    }
}
